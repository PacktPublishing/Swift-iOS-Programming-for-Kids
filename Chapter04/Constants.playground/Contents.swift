var favoriteColor = "Green"

favoriteColor = "Blue"

print(favoriteColor)


let momsName = "Maryann"

// This is in error! Why? Because momsName is defined with the let keyword (which means it's a constant). In order for this code to work, we would need to change how momsName is declared. We would need to change it to a variable (using the var keyword).
momsName = "Patty"

     
